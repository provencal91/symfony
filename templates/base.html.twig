<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <header>
        {% block navbar %}
        {{ include('wild/navbar.html.twig') }}
        {% endblock %}

    </header>
        {% block body %}{% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
   <br>
    <a href="{{ path('wild_show', { 'slug': "walking-dead" }) }}">
        Testing show() method from WildController with a real program s slug.
    </a>
    <br>
    <a href="{{ path('wild_index') }}">
        Testing index() method from WildController to view all programs.
    </a>
    <br>
    <a href="{{ path('wild_category', {'categoryName': "horreur"}) }}">
        Testing showByCategory() method from WildController to view the last three programs.
    </a>
    <br>
    <a href="{{ path('wild_season', { 'slug': "the-big-bang-theory",'id': "1"}) }}">
        Testing showBySeason() method from WildController to view the one season.
    </a>
    </body>
</html>
